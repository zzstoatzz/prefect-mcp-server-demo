this is my prefect project

use the Prefect MCP server for
- read-only access to the state of the Prefect server/workspace
- docs search to understand concepts and how to use the library


NEVER use `uv pip`, its NEVER necessary. `uv sync` and `uv run` will do the right thing.